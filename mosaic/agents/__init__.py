"""MOSAIC agent layer.

Phase 0 Day 4 lands ``utils.macro_tools`` (Layer-1 macro). Phase 2 will add
``layer1`` / ``layer2`` / ``layer3`` / ``layer4`` orchestration plus the rest
of the ``utils.*_tools`` modules.
"""
